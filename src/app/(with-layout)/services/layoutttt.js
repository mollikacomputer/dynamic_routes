export const metadata ={
    title:"Service||Ranjit",
    description:"meta description Service description for meta data",
};

const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;